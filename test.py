import math
import signal
import numpy as np  
from matplotlib import pyplot as plt 

# index = np.arange(10)
# rng = np.random.default_rng()
# x = rng.random(10)
# a = np.ma.MaskedArray(index, mask=[False]*5+[True]*5, copy=False).compressed()
# a[0] = 100
# print(index, a)
# x = np.arange(10)
plt.plot(1, 2,'', label='1', color='C0')
plt.plot(1, 2,'', label='1', color='C0')

plt.legend('1', labelcolor='C0')
plt.show()